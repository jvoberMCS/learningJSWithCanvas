/*
Setup.  You shouldn't have to touch any of this
*/
import { initCanvas } from './canvas.js'

document.addEventListener('DOMContentLoaded', () => {
	const canvas = document.getElementById('myCanvas')
	initCanvas(canvas)
})
